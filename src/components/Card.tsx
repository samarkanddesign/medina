import styled from 'react-emotion';

export const Card = styled('div')`
  background: #fff;
  border-radius: 4px;
  box-shadow: 0 1px 2px 0 hsla(0, 0%, 0%, 0.1);
  padding: 1rem;
`;

export default Card;
